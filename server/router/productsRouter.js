const express = require("express");
const router = express.Router();
// const {
//   allOffers,
//   recruiterOffers,
//   addOffer,
//   updateOffer,
//   deleteOffer,
// } = require("../controllers/jobsControllers");

// const { verify_token } = require("../middleware/verifyTokenMiddleware");

// router.get("/all", verify_token, allOffers);

// router.get("/mine", verify_token, recruiterOffers);

// router.post("/add", verify_token, addOffer);

// router.post("/update", verify_token, updateOffer);

// router.delete("/delete/:offerId", verify_token, deleteOffer);

module.exports = router;